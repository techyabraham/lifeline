<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== REQUIRED PERMISSIONS FOR LIFELINE ==== -->
    <!-- Phone Call -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Background location (OPTIONAL, only needed for continuous tracking) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/> -->

    <!-- SMS for notifying emergency contacts -->
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <!-- Internet for API calls, ads, analytics -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Prevents OS from killing the app too easily during emergency operations -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- Smooth gesture navigation, prevents edge conflicts -->
    <uses-feature android:name="android.software.leanback" android:required="false"/>

    <!-- Allow querying call apps on Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.CALL"/>
            <data android:scheme="tel"/>
        </intent>

        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>

        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="smsto"/>
        </intent>

        <!-- Existing Flutter text processing -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="LifeLine"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:allowBackup="true">

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- Enable immersive mode support -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableSystemChrome"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Used by Flutter; do not remove -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
